void main() {
  var a = 5;
  // a = "apple"; // Cannot do this, because binding is done during the comile time itself.
  
  dynamic b = 7;
  b = "apple"; // This we can do , because in this case binding will done during runtime.

  int aq;
  double c;
  String pp;
}
